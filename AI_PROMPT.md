# AI Промпт для создания тестовых примеров

Этот файл содержит инструкции для AI-ассистента по созданию новых тестовых примеров для проекта Test Samples.

## 📋 О проекте

**Test Samples** - коллекция статических HTML-страниц с тестовыми примерами для проверки функциональности автоматизированных тестов. Проект размещается на **GitHub Pages** и должен работать без серверной части, баз данных или сборщиков.

## 🏗️ Структура проекта

```
test-samples/
├── index.html              # Главная страница со списком всех примеров
├── AI_PROMPT.md           # Этот файл (не отображается на GitHub Pages)
├── README.md              # Документация проекта
├── delayed-elements/      # Пример: Задержка загрузки элементов
│   └── index.html
├── duplicate-elements/    # Пример: Множество одинаковых элементов
│   └── index.html
├── input-variations/      # Пример: Вариации полей ввода
│   └── index.html
├── draft-editor-input/    # Пример: Поле ввода в стиле Draft.js
│   └── index.html
├── datetime-shifts/       # Пример: Примеры дат и времени со сдвигами
│   └── index.html
├── font-variations/       # Пример: Вариации шрифтов
│   └── index.html
├── time-variables/        # Пример: Фиксированные и динамические переменные времени
│   └── index.html
├── sequence-comparison/   # Пример: Последовательности и сравнения
│   └── index.html
├── corner-positions/      # Пример: Позиции в углах
│   └── index.html
├── compass-directions/    # Пример: Стороны света
│   └── index.html
├── relative-positions/    # Пример: Относительные позиции
│   └── index.html
├── grid-positions/        # Пример: Позиции в сетке
│   └── index.html
└── ...
```

**Важно:** При добавлении нового примера обновите эту структуру!

## 📐 Правила создания новых примеров

### 1. Структура файлов

- Каждый пример создаётся в **отдельной папке** с понятным названием на английском (kebab-case)
- В папке примера обязательно должен быть файл `index.html`
- Все стили и скрипты должны быть **встроенными** (inline) - никаких внешних CSS/JS файлов
- Никаких зависимостей, фреймворков или сборщиков
- Обязательно добавить favicon 🧪 в виде SVG data URI в `<head>`

### 2. Дизайн примеров

**Обязательные элементы дизайна:**
- Градиентный фон: `background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);`
- Белые карточки с тенями для контента
- Шрифт: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif`
- Кнопка "← Назад" в левом верхнем углу с ссылкой на `../`
- Современный, минималистичный UI
- **НЕ создавать шапку (header) на странице примера** — вся информация о примере уже есть на главной странице в карточке, поэтому шапка не нужна

**Требования к вёрстке:**
- Контент должен помещаться на одном экране без скролла (если возможно)
- Адаптивный дизайн
- Отступы: 30px 20px от краёв страницы
- Максимальная ширина контейнера: 1000-1200px

**Пример структуры HTML:**
```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Название примера - Test Samples</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='0.9em' font-size='90'>🧪</text></svg>">
    <style>
        /* Встроенные стили */
    </style>
</head>
<body>
    <div class="container">
        <a href="../" class="back-link">← Назад</a>
        <!-- Контент примера -->
    </div>
    <script>
        /* Встроенный JavaScript */
    </script>
</body>
</html>
```

### 3. Добавление примера в главную страницу

После создания нового примера необходимо добавить карточку на главную страницу `index.html`:

```html
<a href="example-name/" class="card">
    <div class="card-icon">🎯</div>
    <h2 class="card-title">Название примера</h2>
    <p class="card-description">
        Краткое описание того, что делает этот пример
        и для чего он предназначен.
    </p>
    <div class="card-tags">
        <span class="tag timing">Тэг 1</span>
        <span class="tag loading">Тэг 2</span>
        <span class="tag performance">Тэг 3</span>
    </div>
</a>
```

**Доступные классы для тэгов:**
- `.tag.timing` - голубой (для примеров с таймингами)
- `.tag.loading` - фиолетовый (для примеров с загрузкой)
- `.tag.performance` - оранжевый (для примеров с производительностью)
- `.tag` - серый (для общих тэгов)

### 4. Обновление README.md

После создания примера обязательно добавьте его описание в раздел "Примеры" файла `README.md`:

```markdown
### [Название примера](example-name/)

Краткое описание примера в 2-3 предложениях. Не пишите слишком много.
```

**Важно:** Описание должно быть лаконичным, без лишних подробностей. Просто суть того, что делает пример.

### 5. Обновление AI_PROMPT.md

После создания нового примера обновите **этот файл** (AI_PROMPT.md):

1. **Обновите раздел "Структура проекта"** - добавьте новую папку в дерево:
```
├── example-name/          # Папка с конкретным примером
│   └── index.html         # Страница примера
```

2. **Добавьте описание в раздел "Существующие примеры"**:
```markdown
### Название примера (`example-name`)

- **Описание:** Что делает пример
- **Особенности:** Ключевые фичи примера
- **Применение:** Для чего используется
```

## 🎨 Рекомендации по типам примеров

### Примеры для тестирования UI

- Различные состояния кнопок (disabled, loading, success, error)
- Формы с валидацией
- Модальные окна и всплывающие уведомления
- Drag & Drop элементы
- Динамические списки

### Примеры для тестирования производительности

- Задержки загрузки элементов
- Большие списки с пагинацией
- Ленивая загрузка изображений
- Анимации и переходы

### Примеры для тестирования взаимодействий

- Клики по различным элементам
- Заполнение форм
- Переключение между вкладками
- Навигация по страницам

### Примеры для тестирования таймингов

- Элементы с задержкой появления
- Автоматические переходы
- Таймеры и countdown
- Последовательные анимации

## 🚀 GitHub Pages

**Важно:** Проект разворачивается на GitHub Pages, поэтому:

1. Все пути должны быть **относительными** (не абсолютными)
2. Ссылки между страницами используют относительные пути (`../`, `./example/`)
3. Никаких серверных технологий (PHP, Node.js, Python и т.д.)
4. Только статический HTML, CSS и JavaScript
5. Все ресурсы встроены в HTML (никаких внешних файлов, кроме других HTML страниц)

## ✅ Чек-лист создания нового примера

- [ ] Создана папка с примером в `test-samples/example-name/`
- [ ] Создан файл `test-samples/example-name/index.html`
- [ ] Все стили и скрипты встроены в HTML
- [ ] Добавлен favicon 🧪 (SVG data URI)
- [ ] Добавлена кнопка "← Назад" со ссылкой `../`
- [ ] Дизайн соответствует стилю проекта (градиент, белые карточки)
- [ ] Контент помещается без скролла (если возможно)
- [ ] Добавлена карточка примера в `test-samples/index.html`
- [ ] Обновлён файл `test-samples/README.md` с кратким описанием примера (2-3 предложения)
- [ ] Обновлён файл `test-samples/AI_PROMPT.md` - добавлена папка в структуру и описание в "Существующие примеры"
- [ ] Проверена работа в браузере локально
- [ ] Все пути относительные

## 💡 Пример использования этого промпта

**Запрос пользователя:**
"Создай пример с формой, которая имеет различные типы валидации"

**Действия AI:**
1. Создать папку `test-samples/form-validation/`
2. Создать `test-samples/form-validation/index.html` с:
   - Градиентным фоном
   - Формой с различными полями
   - Встроенными стилями и скриптами
   - Кнопкой "← Назад"
   - Логикой валидации на JavaScript
3. Добавить карточку в `test-samples/index.html`
4. Обновить `test-samples/README.md` (добавить краткое описание в 2-3 предложения)
5. Обновить `test-samples/AI_PROMPT.md` (добавить папку в структуру и описание в "Существующие примеры")

## 🎯 Существующие примеры

### Задержка загрузки элементов (`delayed-elements`)

- **Описание:** 7 элементов загружаются последовательно с задержками от 5 до 80 секунд
- **Особенности:** Обратный отсчёт на каждом элементе, квадратные плитки в grid layout
- **Применение:** Тестирование механизмов ожидания загрузки, проверка таймаутов

### Множество одинаковых элементов (`duplicate-elements`)

- **Описание:** Страница с множеством кнопок "Удалить" разных размеров и форм, но с одинаковым текстом
- **Особенности:** Таблица с кнопками удаления в столбце, контролы управления над таблицей с иконкой корзины, около 20 одинаковых элементов
- **Применение:** Тестирование механизмов поиска элементов среди множества похожих, проверка точности выбора нужного элемента

### Вариации полей ввода (`input-variations`)

- **Описание:** 30 различных дизайнов полей для ввода с разнообразными стилями и расположением элементов
- **Особенности:** Стандартные поля с рамками, поля без границ, с нижним подчеркиванием, лейблы в разных позициях (слева, справа, сверху, снизу), поля с иконками, подписями, градиентными рамками, тенями и другими необычными стилями
- **Применение:** Тестирование механизмов поиска и взаимодействия с полями ввода различных дизайнов, проверка работы с нестандартными UI-элементами

### Поле ввода в стиле Draft.js (`draft-editor-input`)

- **Описание:** Пример поля ввода, реализованного через contenteditable div, как в редакторе Draft.js
- **Особенности:** Поле автоматически расширяется по высоте при вводе текста, поддерживает перенос слов, placeholder реализован через CSS псевдоэлемент, все введенное содержимое всегда видно
- **Применение:** Тестирование механизмов работы с contenteditable элементами, проверка взаимодействия с нестандартными полями ввода, валидация работы с редактируемыми div элементами

### Примеры дат и времени (`datetime-shifts`)

- **Описание:** Страница с различными примерами дат и времени с разными сдвигами: текущая дата и время, сдвиги на дни, часы, месяцы и годы вперёд и назад
- **Особенности:** Каждый пример подписан с указанием направления и величины сдвига (вперёд/назад), значения обновляются каждую секунду, поддерживаются различные форматы (дата, время, дата и время), комбинированные сдвиги
- **Применение:** Тестирование системных переменных платформы, проверка правильности расчёта времени с различными сдвигами, валидация работы с датами и временем

### Вариации шрифтов (`font-variations`)

- **Описание:** Страница с фразой "съешь еще съешь же еще этих мягких французских булок да выпить чаю", написанной 20 раз разными шрифтами
- **Особенности:** Каждая вариация подписана названием шрифта, под блоком находится большое текстовое поле для вывода всех вариаций, размер поля рассчитан так, чтобы поместилось все 20 вариаций без скролла страницы
- **Применение:** Тестирование механизмов работы с различными шрифтами, проверка распознавания текста в разных стилях, валидация работы с текстовыми полями

### Фиксированные и динамические переменные времени (`time-variables`)

- **Описание:** Страница с 4 парами полей для тестирования фиксированных и динамических переменных времени
- **Особенности:** Фиксированная переменная вычисляется один раз в начале таскейса и остается неизменной во всех полях, динамическая переменная обновляется в начале каждого шага с задержками (3, 6, 9 секунд), визуальное различие между типами переменных (цветовая схема), легенда с объяснением поведения переменных
- **Применение:** Тестирование поведения платформы с динамическими и фиксированными переменными для времени, проверка правильности вычисления времени в начале таскейса и шагов, валидация работы с системными переменными времени

### Последовательности и сравнения (`sequence-comparison`)

- **Описание:** Страница для проверки понимания последовательностей и сравнений "больше-меньше" при описании элементов
- **Особенности:** Первая строка - кнопки разного размера, выстроенные по порядку (от больших к маленьким), вторая строка - три одинаковые кнопки (слева, посередине, справа), третий вариант - объект в центре с кнопками слева и справа, четвертая строка - последовательность карточек с логотипами, при клике карточка меняет фон и показывается уведомление о выборе
- **Применение:** Тестирование механизмов понимания последовательностей элементов, проверка способности модели различать элементы по размеру и позиции, валидация работы с относительными описаниями (слева, справа, первая, вторая и т.д.)

### Позиции в углах (`corner-positions`)

- **Описание:** Страница с кнопками, расположенными в углах контейнера
- **Особенности:** Все кнопки имеют одинаковый текст "Кнопка", различаются только по позиции: левый верхний угол, правый верхний угол, левый нижний угол, правый нижний угол, кнопки позиционированы абсолютно относительно контейнера
- **Применение:** Тестирование понимания позиций в углах экрана/контейнера, проверка способности модели находить элементы по описанию угловых позиций

### Стороны света (`compass-directions`)

- **Описание:** Страница с центральным объектом и кнопками, расположенными по сторонам света относительно центра
- **Особенности:** Центральный объект в середине контейнера, 8 кнопок вокруг него по сторонам света (север, юг, восток, запад, северо-восток, северо-запад, юго-восток, юго-запад), все кнопки имеют одинаковый текст
- **Применение:** Тестирование понимания направлений по сторонам света, проверка способности модели находить элементы по описанию направлений (север, юго-запад и т.д.)

### Относительные позиции (`relative-positions`)

- **Описание:** Страница с примерами относительного позиционирования элементов
- **Особенности:** Три примера с центральным объектом и кнопками относительно него: слева/справа, выше/ниже, слева выше/справа ниже, все кнопки имеют одинаковый текст "Кнопка"
- **Применение:** Тестирование понимания относительных позиций (слева от, справа от, выше, ниже), проверка способности модели находить элементы по описанию относительного расположения

### Позиции в сетке (`grid-positions`)

- **Описание:** Страница с сеткой элементов для проверки понимания позиций в матрице
- **Особенности:** Сетка 4x4 элементов, все элементы имеют одинаковый текст "Элемент", различаются только по позиции в сетке (первая строка, второй столбец и т.д.), при клике элемент меняет цвет на зелёный
- **Применение:** Тестирование понимания позиций в сетке/матрице, проверка способности модели находить элементы по описанию строки и столбца

---

**Следуйте этим инструкциям при создании новых примеров для проекта Test Samples.**

